# Railway 部署配置
# https://docs.railway.app/deploy/deployments

[build]
builder = "NIXPACKS"
buildCommand = "npm install"

[deploy]
startCommand = "bash scripts/ensure-db.sh && npm run server"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
