# Railway 部署配置
# https://docs.railway.app/deploy/deployments

[build]
builder = "NIXPACKS"
buildCommand = "npm install"

[deploy]
startCommand = "node scripts/ensure-db.js && npm run server"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
