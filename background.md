用户2:
arcgis  pro  arcpy是他的Python包
shp格式的文件
wgs1984  地理坐标系
全国一共是有17万个网格  然后每个网格上悬浮或者点选的时候会显示他的详情数据

用户1：
paper_data.geodatabase 这是我想要放在网站上的数据~

用户1：
点击网格的一个格子之后  我想要展示他对应的具体内容  数据在每个shp文件的属性表里~

我：
昨天我研究了，这个 .geodatabase 的文件其实是 SQLite 数据库; 它数据库里面有很多表 (其实他们就是shp是吗)
我强行改扩展名为 geodatabase.db 看了下里面的数据，应该就三个表有用
- China_city_pg 中国地图的 shp
- China_city_cell 这是我们要的一个个“小方格”吧 我看里面有177611个记录
- China_city_POI_JS2020 小方格点击以后的详情？
但是这数据库一百多兆 不能直接在前端用 浏览器受不了 会卡住渲染，只能在后端先对数据库预处理分片 获得类似于 .mbtiles (也是一种 SQLite)，然后运行一个数据服务接口 允许前端实时按需查询折分之后的数据库 (NodeJS 就行)

前端自然也不是十多万数据格都放在屏幕上，屏幕上总共都没那么多像素 鼠标点不着…用的时候 肯定是放大看某个行政区局部的几百个格子吧，而且就算缩小看到整个中国的时候 无法精确选到某个格子
于是就得前端实现 按用户视口剔除 按需请求数据分片 (OpenLayers 或者 Mapbox GL 什么的)

因此这是一个 Web 全栈项目，最终目的是前端进行地图数据可视化和交互，而配合地理信息数据需要有一个 Node 服务器



用户1：
不用看到所有的格子，就是缩小的时候，只其实所有的格子和所有所有的点在缩小的时候，其实都不用看的。就放大到某一个城市的时候，然后能看到这个城市的格子和点就可以了。

它那个属性表里面就是我想要展示的内容，但是不用展示这么多。这是我这个实验做的所有的数据，到时候如果展示的话，可能就展示6~7条儿就行了。

用户2:
我觉得比较理想的情况就是只有当用户缩放到具体的某个省份大小的时候才开始展示城市  然后缩放到搜城市大小的时候  再开始展示网格  就想高德地图一样

因为其实有个界面 主要是考虑到叠buff给论文审稿人看，主要是好看丝滑，有没有那么好用其实都不重要

---

进一步确认需求后，可视化的内容就是这些：

1. 区域划分(静态底图)
- China_city_pl 是中国国境线，必须画在地图上
- China_city_pg 是中国的行政区划
2. 详细数据(可交互元素)
- China_city_cell 是把中国城市区域按照1km的网格分割而来
  - 表的字段除了地理坐标，最终只需要这几列展示：wpop change. pop6-11 change. pop12-14change. ed ps change. 和ed js change
  - 点击某个cell就显示这个cell的信息的浮层
- China_city_POI_JS2020 是2020年城市初中的点：
  - 表的字段除了地理坐标，最终只需要这几列展示：name和survive pop change就行 只展示这两列的值
  - 点击某个poi就显示这个poi的信息的浮层